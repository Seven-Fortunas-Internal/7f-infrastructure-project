name: Test Grace Period Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: |
          echo "This workflow is for testing NFR-4.6 grace period"
          exit 1  # Intentional failure
